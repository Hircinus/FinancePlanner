<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="menuBtn">
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">18sp</item>
        <item name="android:backgroundTint">@color/blue_800</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>
    <style name="budget_center_layout">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_margin">5dp</item>
    </style>
    <style name="budget_name_layout">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">18sp</item>
        <item name="android:layout_margin">5dp</item>
    </style>
    <style name="budget_item_layout">
        <item name="android:textSize">15sp</item>
        <item name="android:layout_margin">5dp</item>
    </style>
    <style name="settingsSwitch">
        <!-- Customize the thumb color when the switch is in the checked state -->
        <item name="colorControlActivated">@color/green_confirm</item>

        <!-- Customize the thumb color when the switch is in the unchecked state -->
        <item name="colorSwitchThumbNormal">@color/red_decline</item>

        <!-- Customize the track color
        <item name="colorSurface">@color/your_track_color</item>

            Customize the track color when the switch is in the checked state
        <item name="colorSecondary">@color/your_track_checked_color</item>-->
    </style>

</resources>